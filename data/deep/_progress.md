# 逐章深講進度

一章一個完整 pipeline（見 `~/.claude/skills/kittel-chapter/SKILL.md`）。
依書本順序推進。每章記：完成日期、小節數、中文字數、核對抓到幾條。

| 章 | 章名 | 完成 | 小節 | 中文字 | 核對 blocker / minor |
|---|---|---|---|---|---|
| 01 | 晶體結構 Crystal Structure | 2026-09-22 | 5 | 41,700 | 14 / 37 |
| 02 | 波繞射與倒晶格 Wave Diffraction and the Reciprocal Lattice | 2026-09-22 | 5 | 68,427 | 11 / 40 |
| 03 | 晶體鍵結與彈性常數 Crystal Binding and Elastic Constants | 2026-09-22 | 6 | 94,737 | 24 / 63 |
| 04 | 聲子 I：晶格振動 Phonons I. Crystal Vibrations | 2026-09-22 | 5 | 78,884 | 17 / 43 |
| 05 | 聲子 II：熱性質 Phonons II. Thermal Properties | 2026-09-22 | 6 | 100,962 | 18 / 49 |
| 06 | 自由電子費米氣 Free Electron Fermi Gas | 2026-09-26 | 6 | 86,590 | 9 / 63 |

## Ch2 核對抓到的最嚴重三條

1. **峰寬 ∝ 1/N 的維度錯誤**（E 節 (39)）：symbols 把 N 定義為「晶體中的晶胞數」，
   峰寬正比的卻是**線性方向**的晶胞數 M = N^(1/3)。Problem 4 證的是 ε = 2π/M。
   照原稿寫法，三維積分強度變成 N²·(1/N)³ ∝ 1/N，與同段「積分強度 ∝ 樣品體積」打架。
2. **(200) 被說成不是合法面指標**（C 節 p.32）：書 p.12 的 Miller 規則原文是
   “usually the smallest three integers”，並非強制約到最簡；同頁 Fig. 14 就直接
   標了 (200)，圖說寫 “The plane (200) is parallel to (100)”。原稿把自訂約定當成書本約定。
3. **Fig. 13 的球位讀錯**（D 節 p.36）：原稿說立方框架「角上與面心上各有一顆小球」，
   照此外圍應有 8+6=14 顆；實際放大算繪只有 12 顆，位在 12 條**稜的中點**，
   即 cuboctahedron 的 12 個最近鄰倒晶格點。原稿同段後文又說頂點頂到面心，自相矛盾。

## Ch1 核對抓到的最嚴重三條

1. **centered hexagonal 的數學錯誤**（B 節）：原稿說在六方菱形 cell 中心加點會得到
   邊長 a/√3、轉 30° 的六方格。實算 |c| = a/2，最短正交基底為 a/2 與 (√3/2)a，
   是**矩形格**，六重軸已被破壞。結論碰巧仍成立但理由與數字全錯。
2. **SiC 393R 的 3 倍連鎖錯誤**（E 節）：393 是**六方晶胞**層數，R 代表菱方格心，
   原始晶胞只有 393/3 = 131 層 = 262 原子。原稿的 786 原子、2355 支光學分支、
   「比 Al 大 500 倍」全部連帶錯誤。
3. **上橫線位置標錯**（C 節）：書中印的是 (hk̄l)，橫線在 k 上，原稿寫成 (hkl̄)。
   該句的全部內容就是「負號放在哪一位的正上方」，標錯等於示範失敗。
   校對者用 PyMuPDF 放大 14 倍目視才抓到——PDF 文字層不帶上橫線。

## Ch3 核對抓到的最嚴重三條

1. **步驟卡被 CSS grid 拆散**（站台層級，非 Ch3 獨有）：`.deriv__steps li` 是 grid
   container，li 裡只要夾了 `<strong>`，每個子節點都各自變成 grid item，正文掉進
   1.9em 的編號欄一行一個字直落。**全站 861 步中 534 步中招**，Ch1／Ch2 已上線頁面
   同樣中招，前兩輪目檢漏掉。ch-03-s6 頁高被撐到 170,069px，修後 69,467px。
   已改為 `display:block` ＋ 絕對定位編號。
2. **式 (8) 的 ± 上下符號寫反**（s2）：原書式 (7) 對稱模力常數為 C − 2e²/R³，
   故上方 (+) 對應反對稱模 ω_a。原稿寫反，與同檔 steps 自相矛盾。
3. **書上式 (51) 首項是第 8 版誤植**（s5 p.80）：印成 C₁₄ = 1/S₄₄，正確應為
   C₄₄ = 1/S₄₄。校對者放大算繪確認是原書誤植，不是 PDF 抽取造成的。

## Ch4 核對抓到的最嚴重三條

1. **Fig. 8b 橫座標系統性偏低 0.07–0.10**（s3）：縱座標（頻率）讀得很準，橫座標
   K/K_max ≥ 0.24 的點全部偏低，遠超該區塊自訂的 ±0.05 容許。這一個讀圖偏差往下
   污染了三處數值結論——(24) 的「弦斜率 v ≈ 3.7×10³ m/s、比模型高 30%」、KBr 橫波
   「低估 28%」、縱波「低估 25%」。真實橫座標是 0.345 與 0.357，結論全部要重算。
2. **式 (24) 抄漏根號內的 1/2**（s4）：聲學支斜率寫成 ω ≅ [C/(M₁+M₂)]^{1/2}·Ka，
   正確為 [C/(2(M₁+M₂))]^{1/2}·Ka，聲速差 √2（約 41%）。而且它掛在「由 (24) 給出」
   名下、寫在 tier:"book" 的區塊裡——讀者會當成原書內容。
3. **Fig. 8a 的 LO 與 TO 被說成「中途交叉」**（s3，且在常見誤解表重複一次）：
   兩條光學支從 K = 0 到 K/K_max ≈ 0.44 是**完全簡併重疊**、之後才分岔，全程沒有
   交叉，TO 在整個區間都 ≥ LO。像素量測：0.05–0.35 區間光學筆畫恆為單線寬 13 px。

## Ch5 核對抓到的最嚴重三條

1. **6 個 Markdown 表格沒轉成 `<table>`**（產生器層級，Ch3–Ch5 共 13 處）：`md()` 只做
   行內轉換、不認表格，卻被用在推導步驟、誤解卡與公式的 from/limits/numeric 三處，
   整張表以字面 `| --- | --- |` 洩漏到畫面。已新增 `rich()`（含表格走 `prose()`，
   否則走 `md()`），並把公式 meta 的 `<span>` 改 `<div>`（span 不能裝 table）、
   含表格的列改單欄且解除行長上限。修完全站多出 30 張表。
2. **Debye θ 裡 N 的定義前後不一致**：p.112 用原胞數、p.113 明寫 "the number of
   atoms"，矽兩者差 2 倍。原稿混用導致數值欄算錯。已統一為 p.113 的原子數，
   並在式 (23)/(28) 註明兩種計數的關係與換算。
3. **偽造的書內交叉引用**：原稿寫「第 6、7 章的電子態密度 D(ε) 分母同樣是 |∇_k ε|
   …Van Hove 奇異點」。校對者抓下 p.120–280 全文 grep，"Van Hove"／"critical point"
   ／"saddle point" **零命中**。已改寫為明示的本站外推。

## Ch6 核對抓到的最嚴重三條

1. **全站 42 頁在手機 375px 橫向溢出**（Ch1 起就有，目視關卡首次抓到）：`.layout` 等
   grid 容器用 `1fr`，而 `1fr` 的最小值是 min-content，一張寬表或一條長公式就能撐破視窗。
   另有兩個加乘元凶：`.eq__s` 設了 `word-break: keep-all`，讓幾乎沒有空格的中文符號說明
   變成不可斷長串；`.eq__r` 式號欄 `nowrap` 又裝了舊資料的長註記。全部改 `minmax(0,1fr)`、
   說明改正常斷行、式號可換行並在窄螢幕移到公式下方。**375px 與 320px 皆 68 頁零溢出。**
   先前截圖工具的 mobile 那張只拍到頁首，所以一直沒發現——新增 `tools/mobile_overflow.py`
   量 `document.scrollWidth`。
2. **電子熱容「只占 1–2%」的過度概括**（s3）：對 Cu、W、Al 成立，但過渡金屬以低溫 γ
   估算可達 4–11%（Ti／Ru 4.0%、Co 5.7%、Ta 7.1%、Pt 8.2%、Ni 8.4%、V／Pd 約 11%）。
   **這條直接關係到 FinFET 的金屬閘極與接觸**，已改寫並註明低溫 γ 含電子–聲子增強、
   300 K 會消退。
3. **表格數字被從中間拆斷**：`body` 的 `overflow-wrap: anywhere` 會把 min-content 算成
   單一字元寬，表格自動排版因此能把欄壓到比數字還窄（「−2.619」拆成「−2.61／9」）。
   儲存格改 `normal`，數字內部空格（「13 400」「5 × 10⁵」）改不斷行空格。

## 同時修掉的產生器問題（影響 Ch1–Ch6 全部）

- **清單沒有轉換**：`prose()` 不認 Markdown 清單，330 處以「- 熱容：…」這種帶連字號的
  段落呈現。現在轉成 82 個 `<ul>`、23 個 `<ol>`（有序清單用 `value` 保留原號碼）。
- **`rich()` 壓掉段落**：不含表格的欄位走 `md()`，`

` 被壓成空白而黏成一段。
  現在含多段落、清單、引用、小標、表格任一者都走 `prose()`。
- **表頭一律 nowrap**：長表頭把欄撐寬、把關鍵的末欄推到橫捲後面。改為可換行
  （`keep-all` 保中文詞完整）。桌機需橫捲的表從 11 張降到 2 張（各只超出 15px）。
- **解碼器 `/H5108`**：由 ℰ 改為 ℋ（算繪 p.134 確認為 ℋψ = εψ，全書 21 處皆為哈密頓量）；
  新增 `/H17018` = ☉（p.158 太陽質量）。已上線的 Ch3 當時已手動寫成 ℋ，無影響。

## 下一章

**使用者這次只指定做到第 6 章。** 續做 Ch7 以後需先徵詢。
